<%
    var share_provider = theme.share_provider;
    var share_config =theme['share_'+share_provider] || ''
%>
<% if (share_provider) { %>
<%- partial('_partial/share/'+share_provider, {"share_config":share_config}) %>
<% } %>